Bridge between TNC and local brcmap server
===========================================

Preparing machine
-----------------

        sudo apt-get install git
        cpan App::cpanminus
        cpanm Ham::APRS::FAP
